<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.schoolmarket.mapper.ChatSessionMapper">
    <resultMap id="BaseResultMap" type="com.example.schoolmarket.entity.ChatSession">
        <id column="id" property="id"/>
        <result column="user_id1" property="userId1"/>
        <result column="user_id2" property="userId2"/>
        <result column="last_message" property="lastMessage"/>
        <result column="unread_count1" property="unreadCount1"/>
        <result column="unread_count2" property="unreadCount2"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.schoolmarket.entity.ChatSession">
        INSERT INTO chat_sessions (user_id1, user_id2, last_message, unread_count1, unread_count2) VALUES (#{userId1}, #{userId2}, #{lastMessage}, #{unreadCount1}, #{unreadCount2})
    </insert>

    <update id="update" parameterType="com.example.schoolmarket.entity.ChatSession">
        UPDATE chat_sessions SET last_message = #{lastMessage}, unread_count1 = #{unreadCount1}, unread_count2 = #{unreadCount2}, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM chat_sessions WHERE id = #{id}
    </select>

    <select id="selectByUserIds" resultMap="BaseResultMap">
        SELECT * FROM chat_sessions WHERE (user_id1 = #{userId1} AND user_id2 = #{userId2}) OR (user_id1 = #{userId2} AND user_id2 = #{userId1})
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM chat_sessions WHERE user_id1 = #{userId} OR user_id2 = #{userId} ORDER BY updated_at DESC
    </select>

    <delete id="deleteById">
        DELETE FROM chat_sessions WHERE id = #{id}
    </delete>

    <update id="incrementUnreadCount">
        UPDATE chat_sessions SET unread_count1 = unread_count1 + 1 WHERE id = #{sessionId} AND user_id1 = #{userId}
    </update>

    <update id="clearUnreadCount">
        UPDATE chat_sessions SET unread_count1 = 0 WHERE id = #{sessionId} AND user_id1 = #{userId}
    </update>
</mapper>