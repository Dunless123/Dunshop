<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.schoolmarket.mapper.ChatMessageMapper">
    <resultMap id="BaseResultMap" type="com.example.schoolmarket.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="sender_id" property="senderId"/>
        <result column="receiver_id" property="receiverId"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="message_type" property="messageType"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.schoolmarket.entity.ChatMessage">
        INSERT INTO chat_messages (session_id, sender_id, receiver_id, content, status, message_type) VALUES (#{sessionId}, #{senderId}, #{receiverId}, #{content}, #{status}, #{messageType})
    </insert>

    <update id="update" parameterType="com.example.schoolmarket.entity.ChatMessage">
        UPDATE chat_messages SET content = #{content}, status = #{status}, message_type = #{messageType} WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM chat_messages WHERE id = #{id}
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT * FROM chat_messages WHERE session_id = #{sessionId} ORDER BY created_at ASC LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM chat_messages WHERE session_id = #{sessionId}
    </select>

    <update id="updateStatus">
        UPDATE chat_messages SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateStatusBySessionAndReceiver">
        UPDATE chat_messages SET status = #{status} WHERE session_id = #{sessionId} AND receiver_id = #{receiverId}
    </update>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM chat_messages
    </select>

    <select id="getMessageTrend" resultType="java.util.HashMap">
        SELECT DATE(created_at) as date, COUNT(*) as count FROM chat_messages 
        <where>
            <if test="startDate != null">AND created_at >= #{startDate}</if>
            <if test="endDate != null">AND created_at &lt;= #{endDate}</if>
        </where>
        GROUP BY DATE(created_at) ORDER BY date DESC
    </select>
</mapper>